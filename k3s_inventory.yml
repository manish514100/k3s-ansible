---
k3s_cluster:
  children:
    server:
      hosts:
        192.168.16.227:
    agent:
      hosts:
        192.168.16.228:
        # 192.16.35.13:

  # Required Vars
  vars:
    ansible_port: 22
    ansible_user: manish
    k3s_version: v1.35.0+k3s1
    token: "63f38d91598c1083bb873ea2ae5e8072"
    api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"

    cluster_context: k3s

    server_config_yaml:  |
      disable:
        - traefik
        - servicelb
